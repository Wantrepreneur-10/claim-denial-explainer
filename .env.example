# Optional — the app works without either of these (fallback/retrieval-only mode).
# Set ONE to enable natural-language, LLM-generated explanations.
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
